import 'package:flutter/material.dart';

void main() {
  const a = MaterialApp(
    home: Scaffold(
      body: Center(
        child: Text('花'),
      ), // Center
    ), // Scaffold
  ); // MaterialApp
  runApp(a);
}
